# リアルタイム音声認識＆OBS連携 実装ステップ (Electron版)

このドキュメントは、マイクからの音声をリアルタイムで文字起こしし、その結果をOBSの画面に表示するための実装手順を定義します。
このプロジェクトはElectronアプリケーションであることを前提とします。

## 全体構成

-   **レンダラープロセス (Renderer Process)**: `index.html` + `preload.js` + `renderer.js`
    -   アプリケーションのUIを担当します。(`index.html`)
    -   マイクへのアクセスと音声データの取得を行います。
    -   取得した音声データを **IPC (プロセス間通信)** を経由してメインプロセスに送信します。
    -   メインプロセスから受け取った文字起こし結果を画面に表示します。
-   **メインプロセス (Main Process)**: `main.js`
    -   アプリケーションのライフサイクル管理、ウィンドウの作成などを行います。
    -   Node.jsの全機能にアクセス可能です。
    -   レンダラープロセスからのIPC通信を待ち受け、音声データを受信します。
    -   `onnxruntime-node` を使用して、`/models`内のONNXモデルで推論を実行します。
    -   推論結果（テキスト）をIPC経由でレンダラープロセスに返します。
-   **OBS**:
    -   「ウィンドウキャプチャ」または「ブラウザソース」機能を使用し、Electronアプリの画面を読み込みます。
    -   画面に表示された文字起こし結果を配信にオーバーレイします。

---

## 実装ステップ

### ステップ1: レンダラープロセスの実装 (マイク音声の取得と送信)

1.  **HTMLの準備 (`index.html`)**:
    -   録音開始・停止を制御するボタンを配置します。
    -   文字起こし結果を表示するための`<div>`要素を配置します。

2.  **JavaScriptの準備 (`renderer.js`など)**:
    -   **マイク音声の取得**: `navigator.mediaDevices.getUserMedia` を使用してマイクへのアクセス許可を求め、音声ストリームを取得します。
    -   **音声データの送信**: Web Audio API (`AudioContext`など) を使用してマイクからの音声をバッファリングし、`preload.js`経由で公開されたIPC関数 (`ipcRenderer.send`など) を使ってメインプロセスに送信します。

### ステップ2: メインプロセスの実装 (IPCによるデータ受信)

1.  **ElectronのIPC設定**:
    -   メインプロセス (`main.js`) で、`ipcMain`モジュールを`require`します。
    -   `ipcMain.on('channel-name', (event, data) => { ... })` の形で、レンダラープロセスからの音声データ受信を待ち受けるリスナーを設定します。

2.  **Preloadスクリプトの作成 (`preload.js`)**:
    -   レンダラープロセスとメインプロセスを安全に繋ぐためのスクリプトです。
    -   `contextBridge.exposeInMainWorld` を使い、メインプロセスへデータを送信するための安全な関数をレンダラープロセスの`window`オブジェクトに公開します。

### ステップ3: 音声データの前処理

-   **データ形式の変換**: **メインプロセス側で**、受信した音声データをWhisperモデルが要求する形式（**16kHzサンプリングレート, モノラル, 32ビット浮動小数点数 (Float32) のPCMデータ**）に変換します。
-   レンダラープロセスから送られてくるデータのサンプリングレートが異なる場合（例: 48kHz）、16kHzにリサンプリングする処理が必要です。この処理のために、音声処理ライブラリの導入を検討します。

### ステップ4: ONNXモデルでの推論実行

1.  **ONNX Runtimeの利用**:
    -   メインプロセスで `onnxruntime-node` を `require` します。
    -   `InferenceSession.create()` を使用して、`/models`フォルダにあるWhisperのONNXモデルを非同期で読み込みます。

2.  **推論の実行**:
    -   ステップ3で前処理した`Float32Array`形式の音声データから入力テンソルを作成します。
    -   `session.run()` を呼び出して推論を実行します。
    -   モデルから出力テンソル（デコード前のトークンIDなど）を取得します。

### ステップ5: 推論結果のデコードとテキスト化

-   **トークナイザの実装**: Whisperモデルの出力は、テキストそのものではなく、単語や文字に対応するIDのシーケンス（トークンID）です。
-   このトークンIDを人間が読めるテキストに変換（デコード）するための「トークナイザ」をメインプロセスに実装します。
-   デコードして得られたテキスト文字列を、`window.webContents.send()` を使ってレンダラープロセスに送信します。

### ステップ6: レンダラープロセスでの結果表示

-   **IPCメッセージの受信**: レンダラープロセス側で、`preload.js`経由で公開されたIPCリスナー (`ipcRenderer.on`など) を使って、メインプロセスからの文字起こし結果を受信します。
-   **画面の更新**: メインプロセスからテキストが送られてきたら、`index.html`内の表示用`<div>`要素の中身をそのテキストで更新します。

### ステップ7: OBSとの連携

1.  **OBSにソースを追加**:
    -   OBSの「ソース」パネルで「+」をクリックし、「ウィンドウキャプチャ」を選択して実行中のElectronアプリを指定するのが最も簡単です。
    -   または、従来通り「ブラウザ」を選択し、「URL」に `index.html` のローカルファイルパスを指定することも可能です。
2.  **CSSでのスタイリング (任意)**:
    -   `index.html`にCSSを追加して、文字のフォント、サイズ、色、背景などを調整し、配信画面で見やすいようにデザインします。

